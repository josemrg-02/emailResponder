# emailResponder
An AI-powered customer-support automation built with n8n, OpenAI GPT-4o, and Gmail API. This workflow automatically reads incoming emails, classifies whether they require customer support, and—if so—sends a polite, context-aware reply generated by an AI agent.
